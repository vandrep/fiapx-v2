<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>br.com.fiapx</groupId>
        <artifactId>fiapx</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>fiapx-extracao</artifactId>
    <name>FIAP X :: extracao</name>

    <dependencies>
        <!-- Health check para o depends_on: service_healthy do Compose ja vem do agregador
             (pom.xml raiz), igual aos outros dois servicos: expoe /q/health sem exigir
             borda HTTP publica nenhuma.

             Mensageria com videos (contrato em docs/contratos/mensagens.md, ticket 015). O
             conector RabbitMQ declara topologia (exchange, fila, DLQ, x-delivery-limit) via
             configuracao dos canais. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-messaging-rabbitmq</artifactId>
        </dependency>

        <!-- MinIO por API S3. O netty-nio-client nao e opcional: sem ele o S3AsyncClient nao
             encontra HTTP client no classpath (ticket 005). -->
        <dependency>
            <groupId>io.quarkiverse.amazonservices</groupId>
            <artifactId>quarkus-amazon-s3</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>netty-nio-client</artifactId>
        </dependency>

        <!-- @Retry nos adapters de I/O do MinIO (ADR 0001). Territorio novo neste repo. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-fault-tolerance</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
